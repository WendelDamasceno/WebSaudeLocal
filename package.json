{
  "name": "saude-local-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "install-frontend": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
